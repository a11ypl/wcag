# AGENTS.md

## Scope
This workspace is for the `a11yfirst.pl` course site only.
Work only inside `a11yfirst-course/`.

## Do Not Touch
Do not modify or restore unrelated parts of the repository outside `a11yfirst-course/` unless the user explicitly asks.

## Deployment
Assume the deploy target for this workspace is the static Vercel project rooted at `a11yfirst-course/`.

## Payment
The production flow is currently bank transfer.
Tpay may exist only as backup/reference and must not be re-enabled without explicit user instruction.

## Assets
Use only stable local assets for production. Do not use expiring CDN or social-media image URLs.

## Working Mode
Before larger edits, inspect the files inside `a11yfirst-course/` and keep changes limited to that module.
